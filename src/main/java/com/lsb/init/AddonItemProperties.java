package com.lsb.init;

import com.gempire.init.ModItemProperties;

public class AddonItemProperties {
    public static void addCustomItemProperties() {

        //Jade
        ModItemProperties.makeGem(AddonItems.WHITE_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.ORANGE_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.MAGENTA_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.LIGHT_BLUE_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.YELLOW_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.LIME_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.PINK_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.GRAY_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.LIGHT_GRAY_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.CYAN_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.PURPLE_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.BLUE_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.BROWN_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.GREEN_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.RED_SBJADE_GEM.get());
        ModItemProperties.makeGem(AddonItems.BLACK_SBJADE_GEM.get());
        //PENTAGONITE
        ModItemProperties.makeGem(AddonItems.SBPENTAGONITE_GEM.get());
    }
}
